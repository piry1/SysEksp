export enum Plans {
    Gain,
    Stay,
    Loose
}

export enum Metabolism {
    Ektomorfik,
    Mezomorfik,
    Endomorfik
}

export enum PhisicalActivity {
    None,
    Small,
    Normal,
    Big,
    VerryBig,
    Extreme
}

export enum Gender {
    Male,
    Female
}

export enum BodyFat {
    Lean,
    Ideal,
    Average,
    Above
}

export enum Water {
    Below,
    Norm,
    Abovew
}